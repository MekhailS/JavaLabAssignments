package com.mekhails.lab1;

public class Main
{
    public static void main(String[] Args)
    {


        System.out.println("HELLO WORLD");
        ConfigReader cfgReader = new ConfigReader("config.txt");

        ClonerReversedBits reverser = new ClonerReversedBits(
                cfgReader.getBufferSize(),
                cfgReader.getInputFilename(), cfgReader.getOutputFilename());

        reverser.cloneEachReversedByteBuff();


        ConfigReader cfgReader2 = new ConfigReader("config2.txt");

        ClonerReversedBits reverser2 = new ClonerReversedBits(
                cfgReader2.getBufferSize(),
                cfgReader2.getInputFilename(), cfgReader2.getOutputFilename());

        reverser2.cloneEachReversedByteBuff();

        int k = 0;
    }
}